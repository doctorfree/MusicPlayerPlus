session_name: ${SESSION}
windows:
  - window_name: MusicPlayerPlus
    focus: true
    layout: main-horizontal
    panes:
    - shell_command:
      - ${MPPCOMM}; exit
      focus: true
    - shell_command:
      - ${VIZCOMM}; exit
    options:
      main-pane-height: ${MAIN_PANE_HEIGHT}
