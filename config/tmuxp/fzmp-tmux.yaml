session_name: fzmp
windows:
  - window_name: Fuzzy Finder
    layout: main-horizontal
    panes:
    - shell_command:
      - fzmp -a; exit
      focus: true
    - shell_command:
      - mppcava -p ${HOME}/.config/mppcava/config-tmux; exit
    options:
      main-pane-height: 20
