# MPD Per-User Playlists

The MPD playlist directory stores MPD created playlists.

The default configuration sets this to `~/.config/mpd/playlists`.

Edit `~/.config/mpd/mpd.conf` to customize this location.
